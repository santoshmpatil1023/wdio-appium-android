/**
 * Week 2 Day 8 — register custom WDIO commands here, e.g.:
 * browser.addCommand('scrollUntilVisible', async function (selector) { ... })
 */

export function registerCustomCommands() {
    // placeholder for scrollUntilVisible, longPressElement, etc.
}
